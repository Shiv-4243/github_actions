name: github_environment

on: 
  workflow_dispatch:

jobs:
  name: printing secrets
  runs-on: ubuntu-latest

  environment:
    name: prod
    url: http://hithub.com
  
  steps:
   - run:
      echo "my_sercret: ${{secrerts.code_kirito}}" 